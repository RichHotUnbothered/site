# Copy to .env for local `vercel dev` testing (gitignored).
# For production, set these in Vercel → Project Settings → Environment Variables instead —
# .env is never read at runtime on Vercel's servers.

AC_API_URL=https://youraccountname.api-us1.com
AC_API_KEY=
AC_LIST_ID=
AC_TAG_ID=
